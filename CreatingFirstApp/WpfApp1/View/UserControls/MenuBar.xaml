<UserControl x:Class="MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             Height="20" d:DesignWidth="800">
    <Grid>
        <Rectangle Grid.Row="0"></Rectangle>
        <Menu>
            <MenuItem></MenuItem>
        </Menu>
        <ComboBox Name="cbLogOut" Grid.Row="0" Text="Logged In" IsEditable="True" IsReadOnly="True" FontSize="7" HorizontalAlignment="right" VerticalAlignment="Center" Width="60" Foreground="DarkBlue" Background="CadetBlue" BorderBrush="DodgerBlue" BorderThickness="2"
            FontWeight="Bold">
            <ComboBoxItem Content="Sign Out"/>
        </ComboBox>
    </Grid>
</UserControl>
